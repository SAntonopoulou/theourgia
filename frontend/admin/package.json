{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@theourgia/admin",
  "version": "0.0.0-dev",
  "private": true,
  "type": "module",
  "description": "Theourgia admin SPA (vault dashboard, Tiptap editor, divination workbench)",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome check src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@theourgia/shared": "workspace:*"
  }
}
