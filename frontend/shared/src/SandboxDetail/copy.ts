/**
 * SandboxDetail · verbatim copy from H09
 * `Theourgia Sandbox Detail.dc.html`.
 */

export const SD_INTRO =
  "Browse the bundle exactly as it would appear in your vault. Every card here is marked — nothing in the sandbox can be mistaken for your own work.";

/** Sandbox card chip — verbatim. */
export const SD_IN_SANDBOX_CHIP = "in sandbox";
export const SD_CHIP_GLYPH = "‡";

/** Expiry callout — verbatim copy template. */
export const SD_EXPIRY_PREFIX = "This sandbox expires ";
export const SD_EXPIRY_SUFFIX =
  ". Promote to main vault or preserve before then.";

/** Same rule-36 disclosure as the Sandbox Browser. */
export const SD_RULE_36_DISCLOSURE =
  "Sandbox content is local to this device. It never federates, never appears in network feeds, never reaches the Fediverse — even if you've enabled federation.";

export const SD_PROMOTE_CTA = "Promote to main";
export const SD_DISCARD_CTA = "Discard";
