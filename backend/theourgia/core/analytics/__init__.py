"""Phase 09 — Synchronicity & Analytics core.

Houses:
  * autotag — the pure on-save tagger for synchronicities (B120)
  * query_dsl — the saved-query DSL parser + validator (B121)
  * executor — the filter-pipeline executor (B122)
  * aggregates — counts, time-series, heatmap, correlation (B123)
  * digest_builder — the weekly digest pipeline (B124)
"""
