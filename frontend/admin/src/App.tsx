import { Foundations } from "./routes/Foundations.js";

/**
 * Theourgia admin shell. Phase 02 Batch 1 ships the Foundations smoke page only;
 * AppShell, routing, and authenticated surfaces land in subsequent batches.
 */
export function App() {
  return <Foundations />;
}
